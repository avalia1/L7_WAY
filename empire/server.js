#!/usr/bin/env node
// Frozen: this path is not a product HTTP server.
console.error('The Empire listener is archived. Start the control plane with: npm start');
console.error('Former server: archive/empire/server.js (do not run as a gateway).');
process.exit(1);
